{
  "name": "brainrot-lab-merge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:yandex": "npm run build && node scripts/zip.mjs",
    "preview": "vite preview",
    "sim": "esbuild scripts/sim.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/sim.mjs --log-level=error && node node_modules/.cache/sim.mjs"
  },
  "dependencies": {
    "@fontsource/rubik": "^5.3.0",
    "phaser": "^3.85.2"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "adm-zip": "^0.6.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
