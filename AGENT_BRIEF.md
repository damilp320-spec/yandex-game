# ПРОМПТ ДЛЯ АГЕНТА (Opus 5): выведи «Brainrot Lab: Merge» в релиз и преврати в живой сервис

Ты — ведущий разработчик и продюсер HTML5-игры **Brainrot Lab: Merge** для платформы Яндекс Игры. Это второй бриф проекта: первый (Фаза 0 — Release Candidate) выполнен почти полностью, игра в состоянии RC. Твоя задача теперь другая: **дожать релиз и построить машину удержания** — контентный ритм, крючки возврата и мид/лейт-гейм, которых у игры пока нет. Работай автономно, по фазам, в порядке приоритетов. Не переспрашивай то, что описано здесь.

---

## 1. КОНТЕКСТ ПРОЕКТА

### 1.1. Репозиторий
- **Репо:** `damilp320-spec/yandex-game`, рабочая ветка `claude/yandex-games-plan-ke763a`, PR #1 в `main` (draft, мержабелен, ревью нет).
- **Стек:** Phaser 3.90 + TypeScript + Vite, шрифт Rubik из `@fontsource` (бандлится), ноль бинарных ассетов — графика процедурная, звук — синтез WebAudio (и то и другое подменяется пользовательскими файлами через манифесты в `public/`).
- **Документы:** `PLAN.md` (гейм-дизайн и психология удержания), `PROMPTS.md` (промпты генерации PNG), `README.md` (запуск/публикация — держи актуальным).
- **Проверки перед каждым коммитом:** `npx tsc --noEmit`, `npm run build`; менял баланс — ещё `npm run sim`. `npm run build:yandex` собирает zip для консоли.

### 1.2. Архитектура (`src/`)
| Файл | Ответственность |
|---|---|
| `config.ts` | ВЕСЬ баланс и «скин»: 12 цепочек, 4 локации, цены, доход, лиги, GRID |
| `state.ts` | Единый стейт `S`, сериализация, миграции сейвов, квесты, стрик |
| `GameScene.ts` | Главная сцена: поле 5×6, HUD, панели, нижняя навигация с герой-кнопкой «Арена» |
| `arena.ts` | Архетипы цепочек, генерация соперников, кубки, лиги, милстоуны |
| `battle.ts` | Автобатлер-сцена: тики атак, HP-бары, анимации |
| `shop.ts` | Витрина Яндекс.Платежей, синки кристаллов, сундук с секретным «67» |
| `sdk.ts` | Обёртка Yandex SDK: реклама, сейвы, платежи, лидерборды — всё с dev-моками |
| `sprites.ts` / `assets.ts` | Процедурные спрайты 128px / подмена PNG через manifest |
| `audio.ts` | Синтез-джинглы / подмена mp3 через manifest |
| `events.ts` | Календарь сезонных событий, конфиг-драйвен |
| `analytics.ts` | Яндекс.Метрика (`METRICA_ID` в config, сейчас 0 = консоль) |
| `i18n.ts` | RU/EN/TR, ~200 ключей, `t('key', {params})` |
| `scripts/sim.ts` | Детерминированная симуляция: 14 дней экономики + тысячи боёв (`npm run sim`, mulberry32, SEED=20260726) |

### 1.3. Что реализовано (НЕ ломай, это фундамент)
- **Экономика-кликер:** пассивный доход существ раз в 5с; тап с комбо ×5; «золотой брейнрот» ~90с; бусты ×2 (rewarded) / ×3 (кристаллы); офлайн в двух уровнях — 50%/2ч бесплатно, 100%/5ч с покупкой `offline_vip`, rewarded ×2 поверх обоих.
- **Merge-кор:** 12 цепочек × 6 уровней, drag&drop, запертый 6-й ряд, четыре локации со своими полями. Добыча — ОДНА кнопка внизу: бесплатно по кулдауну 25с либо за монеты (цена ≈ минута дохода поля, самобалансируется через `spawnCostOf`).
- **Карточка существа** (long-press ≥350мс): доход, боевые статы, продажа (`sellPrice` = 2 минуты дохода), отправка в команду.
- **Арена:** 5v5 автобатлер, архетипы melee/sniper/splash, соперники-симуляции под кубки, лестница с равновесием ~47% побед, реванш ×1.2 за rewarded, казарма (экспоненциальный синк), 6 лиг, 10 порогов наград до 5000🏆 с сундуками, лидерборд `cups`.
- **Удержание:** стрик 7 дней со спасением за рекламу, 4 дневных квеста, Мемпедия, сезонные события (`beach26` активен, `ny26` заготовлен), «Битва недели».
- **Виральность:** секретный «67» (6,7% из сундуков), именование легендарок, share-тексты.
- **Монетизация:** `starter`, `gems_s/m/l`, `no_ads`, `offline_vip`; interstitial только на стыках (после каждого 3-го боя, кап 1/3мин, не в первые 3 минуты сессии).
- **Инфраструктура:** i18n RU/EN/TR, настройки (звук/язык/сброс), FTUE — 4 одноразовые подсказки, симуляция баланса, перф-пасс на утечки (нулевая дельта).

### 1.4. Священные принципы (нарушение = регресс продукта)
1. Retention > краткосрочный доход. Фича роняет D1 — откат.
2. 80%+ рекламы инициирует игрок. Interstitial никогда не внезапный, всегда с паузой игры/звука; награда rewarded — только по onRewarded.
3. **Всё платное дублируется бесплатным путём** (время/монеты/rewarded). Платное покупает удобство и время, никогда — силу в бою.
4. Никакого наказания за отсутствие — только пряник за возврат. Никаких энергий, таймеров-шантажа, «копилок» без бесплатного крана.
5. Вся тематика — в конфигах; смена тренда не требует правок логики.
6. **Правило большого пальца:** всё, что игрок нажимает часто, живёт ниже середины экрана. Верх — для редких взглядов (валюта, таймеры).
7. **Sim-first:** любое изменение экономики/боя сначала прогоняется в `npm run sim`. Баланс цепочек меряется силой, не winrate (снежный ком 5v5 раздувает ±5% силы до счёта 5:95 — это свойство формата, зафиксировано бисекцией).
8. Все строки — через `t()`. Все новые фичи — сразу в трёх локалях.

---

## 2. АНАЛИЗ СОСТОЯНИЯ (июль 2026)

### 2.1. Что у игры сильно
- **Ниша свободна:** merge+idle+автобатлер на Яндекс Играх не занята — вокруг примитивные кликеры и раннеры по тому же тренду.
- **Экономический кор глубокий и проверенный:** симуляция показывает здоровую кривую — клуб на день 4, космо-база на день 9 у оптимального игрока, казарма-синк работает, офлайн не доминирует.
- **Арена как «PvP-ощущение» без бэкенда** — главный магнит и главная кнопка экрана.
- **Скорость контента:** новое существо/событие = запись в конфиге, час работы. Это козырь против конкурентов.

### 2.2. Честный аудит: где игра теряет игроков (это твой backlog)
1. ✅ **Причина вернуться завтра** — сделано в Фазе 1: инкубатор (яйца на 2/8/24 ч), мутация дня, тизер завтрашней награды. Остался сезонный журнал → Фаза 2.
2. ✅ **Мид/лейт-гейм появился** — престиж с деревом перков и мифики через fusion (Фаза 3).
3. ✅ **Арена получила календарный ритм** — сезоны с рамками, боссы лиг, турнир выходных, журнал-пасс (Фаза 2).
4. ✅ **Платформенные рычаги Яндекса** — оценка, ярлык, вход в аккаунт: сделаны в Фазе R (`sdk.ts` + `platformPrompt` в GameScene, по одному предложению за сессию).
5. **Арт всё ещё процедурный.** Играбельно, но витрина (иконка, скриншоты, первое касание) проигрывает генеративному арту. Владелец генерирует PNG по `PROMPTS.md` — интегрируй, как только появятся в `public/skins/`.
6. **Бой не проверен на реальном устройстве** (headless даёт ~2 FPS). Сим говорит ~19с на бой при 60 FPS; проверить на телефоне, крутить `BALANCE.dmgConst` только через сим-свип.
7. ✅ **Достижения** — 28 наград на второй вкладке Мемпедии, метрики читаются из сейва (`src/achievements.ts`).
8. **Один слот сейва** — конфликт двух устройств решается «кто последний». Для релиза ок, задокументируй в README.
9. **Мемпедия 12×6 тесновата** на 320px ширины — проверить.
10. **Ручные шаги владельца не сделаны:** METRICA_ID=0, покупки/лидерборды в консоли не созданы, Pages не включены, PR не смержен. Напоминай в каждом отчёте.

### 2.3. Рынок (середина 2026)
- **Тренд брейнрота зрелый:** пик хайпа пройден, аудитория лояльна, мем-машина продолжает рожать персонажей. Стратегия: быстрая реакция на новые мемы (событийная цепочка за 48ч) вместо ставки на старых; готовность к пивоту скина за 1–2 недели (запасной скин — PLAN.md §13).
- **Алгоритм Яндекс Игр** любит возвраты (D1/D7), время в игре, рейтинг и **свежие обновления** (бейдж «Обновлено» + переранжирование). Вывод: после релиза — еженедельный видимый апдейт минимум месяц. Это не опция, это план.
- **Сезонность рекламы:** декабрь eCPM ×1.5–2, январь-февраль провал. Новогодний ивент (`ny26` готов) + пуш контента к началу декабря обязательны.
- **KPI-цели:** D1 ≥35%, D7 ≥12%, D30 ≥5%, сессий/день ≥2.5, rewarded/DAU ≥1.5, ARPDAU ≥0.28₽.

---

## 3. РОАДМАП

Порядок жёсткий: релизное окно новинки на платформе — самый дешёвый трафик, его нельзя тратить на разработку «вглубь». Сначала релиз с быстрыми платформенными рычагами, потом еженедельные крючки, потом глубина.

### ФАЗА R — Релиз (сейчас, ≤1 неделя)
СТАТУС: пункты 1–2 сделаны (рычаги платформы + вибрация), 3 не нужен пока (`public/skins/`
пуст — арта нет). Осталось: смоук на телефоне, финальный build и шаги владельца.

1. ✅ **Платформенные рычаги** (день работы, максимальный ROI):
   - `sdk.ts`: обёртки `canReview()/requestReview()` (ysdk.feedback), `showShortcutPrompt()` (ysdk.shortcut), `isAuthorized()/authorize()` — все с dev-моками.
   - **Запрос оценки:** после первой легендарки ИЛИ клейма милстоуна 300🏆 — что раньше; один раз за установку (`S.reviewAsked`). Момент «вау», не раньше.
   - **Ярлык на рабочий стол:** предлагать на 2-й день игры (новый счётчик `S.daysPlayed`), один раз.
   - **Авторизация ради облака:** если игрок гость — после первой легендарки панель «Не потеряй прогресс» с кнопкой входа. Отказ — не повторять 7 дней.
2. ✅ **Вибрация** (`navigator.vibrate`): 15мс на мердж, 30мс на победу, 10мс на золотого. Флаг `S.hapticsOn`, тумблер в настройках. Дёшево, а «ощущение в пальцах» — половина залипания.
3. **Интеграция арта владельца**, если PNG уже в `public/skins/` (проверь). Нет — не блокируй релиз, процедурка играбельна.
4. Смоук на реальном телефоне (попроси владельца, дай чек-лист: FPS в бою, long-press карточки, драг на маленьком экране, звук после сворачивания).
5. Финальный `build:yandex`, self-check модерации по PLAN.md §5, zip < 5 МБ.
6. **Владелец (напоминай):** METRICA_ID; в консоли — покупки `starter/gems_s/gems_m/gems_l/no_ads/offline_vip`, лидерборды `weekly_merges` и `cups`, иконка 512×512, скриншоты, описания RU/EN/TR с ключами «брейнрот, merge, 67, капибара»; смерж PR #1; включить Pages.

**DoD фазы:** игра на модерации; рычаги оценки/ярлыка/авторизации работают в моках; README обновлён.

### ФАЗА 1 — «Причина вернуться завтра» (релизная неделя +1; каждая фича = отдельный видимый апдейт)
Крючки D1/D7, отсортированы по соотношению эффект/трудозатраты:

СТАТУС ФАЗЫ 1: ✅ ВЫПОЛНЕНА ЦЕЛИКОМ (инкубатор, мутация дня, тизер, достижения,
промокоды). Проверено симуляцией и в браузере; экономика не сдвинулась.

1. ✅ **Инкубатор** (главная фича фазы — «встреча по расписанию», см. спек §4.1).
2. ✅ **Мутация дня** (детерминированный дневной модификатор, спек §4.2).
3. ✅ **Тизер завтрашнего дня:** в панели стрика показывать не только сегодняшнюю награду, но и завтрашнюю («Завтра: 10💎») + после клейма — тост «Возвращайся завтра за X». Одна строка UI, классический прирост D1.
4. ✅ **Достижения** (спек §4.3).
5. ✅ **Промокоды** (спек §4.4) — заодно канал для соцсетей владельца.

### ФАЗА 2 — «Календарный ритм» (недели 2–6 после релиза)
СТАТУС: ✅ ВЫПОЛНЕНА ЦЕЛИКОМ. Всё проверено симуляцией и в браузере. Важные поправки
к исходным спекам — ниже в пунктах.

1. ✅ **Сезон арены** (спек §4.5): месячный цикл, мягкий сброс кубков, награда по пиковой лиге, рамки профиля.
2. ✅ **Лабораторный журнал** — батл-пасс лайт (спек §4.6), сезон синхронизирован с ареной.
3. ✅ **Боссы лиг** (спек §4.7).
4. ✅ **Турнир выходных** (спек §4.8) — но БЕЗ отдельного режима и запаса попыток.
5. ✅ **Косметика** (спек §4.9).

**Что пришлось изменить против спеков (и почему — не повторяй):**
- **Сброс сезона 0.85, а не «половина».** Сброс до 60% отбрасывает игрока на 70 боёв
  (~6 дней) — это наказание за перерыв. При 0.85 возврат ≈26 боёв, но с высоких кубков
  всё равно выбивает из лиги. Пороги наград перенесены на `S.cupsBest`, чтобы сброс
  ничего не отбирал.
- **Боссы бинарны, «40–60% с третьей попытки» недостижимо.** В бою 1×5 снежный ком
  односторонний (босс не теряет урона), поэтому свип по dps даёт 100% и сразу 0%.
  Босс сделан ПРОВЕРКОЙ НА СИЛУ: сим утверждает, что эталонная пятёрка лиги
  проигрывает, а с казармой +5 или уровнем выше — выигрывает. Не пытайся выкрутить
  «честные 50%»: это лезвие ножа, с которого падает любая живая команда.
- **Сплэш против одиночной цели бьёт в 0.45 силы** — эффективный урон команды по боссу
  сильно ниже номинального. Первая калибровка на номинальном DPS дала либо
  неубиваемых боссов, либо бесплатных. Считай эффективный урон (`singleTargetFactor`).
- **Порог тира журнала — 200 очков**, потому что сим намерил 283 очка/день у активного
  игрока (20 тиров ≈ 15 дней, средний игрок ~85% к 30-му дню). Не ставь порог «на глаз».
- **Турнир — надстройка над обычными боями**, а не режим с попытками: параллельная
  экономика попыток (и реклама за них) не давала ничего, кроме сложности.
- **`S.achClaimed` теперь хранит ID, а не индексы.** Достижения добавляются в середину
  списка (порядок = «взросление» игрока), и индексные флаги разъезжались бы. Для
  остальных списков (`arenaClaimed`, `pass.claimed`, `event.claimed`) правило прежнее:
  порядок менять нельзя, только дописывать в конец.

### ФАЗА 3 — «Глубина» (месяц 2–3)
СТАТУС: ✅ ВЫПОЛНЕНА ЦЕЛИКОМ (престиж, мифики + fusion, кланы-лайт).

1. ✅ **Престиж «Новая лаборатория»** (спек §4.10).
2. ✅ **Мифики и fusion** (спек §4.11) — но НЕ как 7-й уровень цепочки: мифик это
   отдельное существо с индексом от `MYTHIC_BASE`, условным уровнем 6 и своим спрайтом.
   Так не пришлось ломать длины цепочек, `discovered`, RARITY и merge-капы.

**Что пришлось изменить против спеков (и почему):**
- **Дерево перков собрано заново по измерению.** Спек предлагал «+10% дохода за уровень»
  как главный перк — сим показал, что он не ускоряет заход ВООБЩЕ (12 → 12 дней): монеты
  в этой экономике не дефицит, к насыщению локации у игрока миллионы свободных. Реальное
  ограничение — поставка существ и число перетаскиваний руками. Ускоряют только кулдаун,
  шанс существа 2-го уровня и шанс двойного слияния (12 → 9 → 8 → 7 дней). Доход оставлен
  для казармы и покупок, но обещать им скорость — неправда.
- **Цель «второй заход 60%, третий 45%» недостижима и это нормально.** Пол — не экономика,
  а `ACTIONS_PER_MIN` (руки игрока): заходы упираются в ~7 дней. Дальше перки продают
  удобство (офлайн, комбо), а не скорость. Сим печатает плато — сверяйся с ним.
- **Мифики переезжают вместе с игроком.** Иначе правило звучало бы «не сливай мификов
  перед престижем» — плохое правило: мифик стоит двух легендарок.
- **Все начисления монет идут через `addCoins`** (state.ts): нейроны считаются от
  заработанного за всё время, и прямое `S.coins +=` ломает счётчик.

3. ✅ **Кланы-лайт («пакт»)** — но НЕ так, как предлагал спек. План был: «код друга = общие
   очки в Битве недели, вклад друга — локальная симуляция». Так делать нельзя: игра
   показывала бы выдуманную активность живого человека как настоящую, а «честно пометить
   бета» этого не исправляет. Реализовано без вранья: у каждого свой код с контрольной
   суммой (чтобы «набрать что попало» не работало), ввёл код друга — получил 30💎, редкое
   яйцо и +5% дохода навсегда, и панель прямо говорит, что награду получает тот, кто ввёл
   код. Виральность от этого не пострадала — код выгодно раздавать, награду получает
   каждый, кто его введёт. Бонус мал и не даёт силы в бою.

### ФАЗА 4 — Экспансия (по данным)
- **Порты:** CrazyGames/Poki — вынести интерфейс поверх `sdk.ts` (реклама/сейвы/платежи как стратегия), сборка под целевой SDK.
- **Настоящий PvP-бэкенд** только при DAU > 20k и арене как главном магните: Yandex Cloud Functions + YDB, защищённое хранение команд. До порога — симуляция строго лучше по ROI.
- **Вторая игра студии** с кросс-промо через Shortcut/ссылки, когда DAU стабилен.

---

## 4. ДЕТАЛЬНЫЕ СПЕКИ ФИЧ

Общие требования к КАЖДОЙ фиче: строки в трёх локалях; события в `analytics.ts`; новые константы в `config.ts` (или свой конфиг-файл); миграция сейва в `ensureShapes()`; если трогает экономику/бой — блок в `scripts/sim.ts`; скриншот через headless (паузы ≥1200мс, таймингам не верить).

### 4.1. Инкубатор 🥚
**Зачем:** «встреча по расписанию» — сильнейший из известных крючков D1 в казуалках. **KPI:** D1, сессий/день.
- **UI:** чип с яйцом и таймером в верхней зоне HUD (верх свободен после удаления заказов; частых тапов тут нет — правилу пальца не противоречит). Один слот.
- **Яйца:** `common` (2ч → существо lvl3 текущей локации), `rare` (8ч → lvl4 + 10💎), `gold` (24ч → lvl5 + 25💎, шанс секрета 6,7%). Если поле забито — компенсация монетами как в `rollChest`.
- **Источники:** каждая 5-я победа на арене → common; повышение лиги → rare; отдельные милстоуны событий и турнира → gold. Яйца — дополнительный кран, НЕ замена сундуков.
- **Ускорение:** rewarded −60 мин (кап 3/день на яйцо), кристаллы 1💎 = 30 мин. Бесплатный путь — просто подождать: принцип 3 соблюдён.
- **Стейт:** `S.egg = { type, startedAt } | null`, `S.eggsHatched`. Конфиг `INCUBATOR` в config.ts.
- **Психология момента:** яйцо, которому осталось <10 мин, подсвечивается; вылупление — фанфары + конфетти. Получил яйцо на 8ч в конце сессии → «загляни перед сном / утром».

### 4.2. Мутация дня 🧬
**Зачем:** дневная вариативность почти бесплатно; повод пересобрать поле. **KPI:** D1, длина сессии.
- Детерминировано от даты (никакого стейта): `chainOfDay = hash(YYYY-MM-DD) % 12` — эта цепочка сегодня приносит **×2 дохода**. Чип «🧬 Сегодня мутирует: Капибарино» над полем, подсветка существ цепочки.
- В день секретной цепочки (выпадет сам раз в ~12 дней) — праздник для коллекционеров, ничего не делать специально.
- Расширение (не сразу): арена-мутация «тип атаки дня +10% силы» — применять ОБЕИМ сторонам боя и прогнать сим-свипом; без сима не трогать.
- Реализация: функция в config.ts + отображение в GameScene + учёт в `incomeOf` вызовах через множитель. Осторожно: `S.incomeRate` для офлайна пишется на момент сейва — мутация в офлайн не входит (иначе игрок «ловит» мутацию сном), зафиксируй это в коде комментарием.

### 4.3. Достижения 🏅
**Зачем:** витрина гордости для уже накопленных счётчиков; средний горизонт целей. **KPI:** D7, длина сессии.
- `achievements.ts`: ~24 записи `{ id, metric, threshold, gems }`. Метрики из существующих и новых lifetime-счётчиков `S.stats` (merges, taps, wins, sold, golden, chests, eggs, legendaries, zones, cupsBest). Добавь недостающие инкременты по месту событий.
- Награда 2–15💎, клейм вручную (кнопка «Забрать» — момент дофамина, не автовыдача).
- UI: вкладка в панели Мемпедии (она — «музей игрока»). Бейдж непрочитанных на кнопке навигации.
- Три «социальных» достижения со смешными названиями для скриншотов («Продал 100 друзей», «67 навсегда», «Спагетти-магнат») — share-текст по клику.

### 4.4. Промокоды 🎟️
**Зачем:** канал владельца в соцсети/описание игры; измеримый источник и подарки аудитории. **KPI:** привлечение, конверсия соцсетей.
- Поле ввода в настройках. Таблица кодов в config: `{ code, gems?, egg?, chest?, until }`. Валидация локальная (списко-кодовая, не криптография — для казуалки достаточно, награды маленькие). `S.codesUsed: string[]`.
- Аналитика `code_redeem` с меткой кода — владелец видит, какой пост сработал.
- Заготовь 5 стартовых кодов и текст-шаблоны постов владельцу (в отчёте, не в коде).

### 4.5. Сезон арены 🗓️
**Зачем:** ежемесячная «новая игра» для верхушки воронки арены, свежий интерес к лестнице. **KPI:** D30, возвраты ветеранов.
- Сезон = календарный месяц (`'2026-08'`). На смене: `S.cups = floor(S.cups * 0.6)` (мягкий сброс к равновесию — matchmaking сам подстроится), `S.cupsBest` (lifetime, НЕ сбрасывается) — по нему живут милстоуны наград, чтобы уже взятое не ломалось.
- Награда в конце по **пиковой лиге сезона** (`S.season = { id, peakLeague }`): от 10💎 (Деревяшка) до 200💎 + рамка (Легенда). Панель «Итоги сезона» при первом входе в новом месяце — это же и момент повторного онбординга в арену.
- **Рамки профиля** по лигам: цветная рамка вокруг аватара в арене и лидерборде. Первая косметика игры.
- Сим: проверь, что после сброса 0.6 игрок с плато возвращается на него за ~15–25 боёв (приятный «сезонный забег», не гринд-стена).

### 4.6. «Лабораторный журнал» — батл-пасс лайт 📔
**Зачем:** главный мид-терм крючок и монетизатор одновременно; индустриальный стандарт удержания. **KPI:** D30, ARPDAU, конверсия в платёж.
- Сезон совпадает с сезоном арены. 20 тиров × 50 очков. Очки: мердж +1, победа +5, клейм квеста +10, золотой +2. Симом проверь: активный игрок закрывает трек за ~3 недели, казуал — 60–70%.
- **Награды free-трека:** монеты номиналом «X минут дохода» (масштабируются как `sellPrice` — фиксированные суммы обесцениваются), кристаллы, сундуки, яйца.
- **Премиум-трек за 450💎** (кристаллы, НЕ новый SKU — это синк уже продаваемой валюты и «мягкая» цена): ×2 к наградам free-трека + эксклюзив на 20-м тире: скин поля + рамка. Силы не даёт (принцип 3: премиум = удобство/косметика; сам трек полностью проходится бесплатно).
- UI: панель с горизонтальным треком, вход с нижней навигации (бейдж прогресса). Тост «+N 📔» на действиях — но не чаще раза в 10с, не спамить.

### 4.7. Боссы лиг 👹
**Зачем:** PvE-вершина для каждой лиги, эмоциональный пик и скриншотный момент. **KPI:** D7, share.
- На входе в новую лигу открывается босс: **5 твоих против 1 гиганта** (HP = 3.5× суммы HP команды, splash-атака, спрайт ×2 — используй существующую сцену боя, `attackPlan` массивы переваривает).
- У каждого босса гиммик из имеющихся механик: «Гранд Эспрессимо» бьёт вдвое чаще, «Мега Крокодиссимо» имеет 20% брони и т.п. — всё выражается существующими статами, новых механик боя НЕ добавлять.
- Награда за первое убийство: gold-яйцо + 30💎 + страница босса в Мемпедии (секретный раздел «Боссы» — крючок коллекционера). Реванш после поражения: бесплатно раз в час или сразу за rewarded.
- Сим: winrate против босса при «ровной по лиге» команде должен быть 40–60% со 2–3 попытки. Крутить только HP-множитель.

### 4.8. Турнир выходных 🏟️
**Зачем:** пик выходного трафика платформы + недельный аппоинтмент. **KPI:** сессии сб-вс, rewarded/DAU.
- Сб-вс: вкладка в панели арены. 10 боёв за день (обычные правила, кубки идут как всегда), счёт = победы. Тиры: 3 победы → монеты (минуты дохода), 6 → 15💎, 9 → rare-яйцо, 10 → сундук. +3 попытки за rewarded (кап 2/день).
- Полностью локально (соперники — те же симуляции), без новых лидербордов — нет зависимости от владельца. Если приживётся (смотри метрику участия) — в следующей итерации лидерборд `weekend_wins` в консоли.

### 4.9. Косметика 🎨
**Зачем:** синк кристаллов без pay-to-win; самовыражение. **KPI:** ARPDAU (через синк → покупку паков), D30.
- **Скины поля** (цвет сетки + частицы фона + цвет подсветки мерджа): 3 штуки по 150–300💎 + 1 эксклюзив пасса + 1 за достижение. Конфиг-драйвен (`SKINS` в config), рендер — параметры уже существующих отрисовок.
- **Рамки профиля** — из сезонов и боссов (не продаются: статус должен быть заработан, это важно для их ценности).
- НЕ делать: скины существ за деньги (конфликт с PNG-артом владельца и с коллекционной логикой Мемпедии).

### 4.10. Престиж «Новая лаборатория» 🔄
**Зачем:** лейт-гейм; превращает «прошёл за 2 недели» в «играю месяцами». Ключевой ответ на аудит-пункт 2. **KPI:** D30+, LTV ядра.
- **Условие:** открыты все 4 локации И космо-база насыщена (≥60% клеток на max-уровне — критерий уже есть в сим) — покажи кнопку «Переезд» с церемонией (грузовик, конфетти).
- **Сбрасывается:** монеты, поля всех локаций, `spawnBought`, замки локаций (кроме лаборатории). **Сохраняется:** кристаллы, Мемпедия, имена легендарок, покупки, кубки/лиги/сезоны, достижения, ЯЙЦО, и — важно — **команда арены** (бойцы и так сняты с поля; иначе престиж убивает арену и никто его не нажмёт).
- **Валюта «Нейроны»:** `floor((lifetimeCoins/1e6)^0.5)` за переезд (новый счётчик `S.lifetimeCoins`). Дерево перков (конфиг): доход +10%/ур., офлайн-часы +30мин/ур. (стакается с VIP), кулдаун спавна −2с/ур. (пол 15с), тап-комбо +1 макс.
- **Сим обязателен:** добавь режим престижа; второй заход до космо-базы должен занимать ~60% времени первого, третий ~45%. Кривая нейронов — чтобы 2-й перк хотелось, а 5-й был мечтой.
- FTUE престижа: одна панель «что сохранится/что сбросится» с явным списком — никакой мелкой типографики, доверие дороже.

### 4.11. Мифики и fusion 🧪 (после престижа, не раньше)
**Зачем:** второй виток коллекции для ветеранов. **KPI:** LTV ядра, share.
- **8 кураторских рецептов** (не 66 комбинаций): легендарка A + легендарка B → мифик с новым именем и спрайтом-гибридом (палитра A + аксессуары B — процедурка это умеет). Рецепты скрыты, в Мемпедии — силуэты с подсказками («??? = кто-то кофейный + кто-то космический»).
- Доход мифика ×3.5 легендарки; в арене — statScale 7-го уровня (прогнать сим!). Мифик занимает клетку как обычное существо.
- Fusion-станция — новая кнопка в карточке легендарки («Слить с…» → выбор второй). Неудачная пара — мягкий отказ с подсказкой-намёком, НЕ трата существ (эксперименты без наказания).

---

## 5. LIVEOPS-КАЛЕНДАРЬ (август–декабрь 2026)

| Когда | Что | Примечание |
|---|---|---|
| Неделя релиза | Релиз + рычаги платформы | `beach26` уже активен до 20.08 |
| Релиз +1 нед | Апдейт: Инкубатор + мутация дня | пост «яйца завезли» |
| +2 нед | Апдейт: Достижения + промокоды | 5 стартовых кодов в соцсети |
| +3 нед | Сезон арены №1 + рамки | «Итоги» придут 1-го числа |
| +4 нед | «Лабораторный журнал» сезон 1 | синхронизирован с ареной |
| Сентябрь | Боссы лиг; турнир выходных | ✅ осенний ивент `fall26` готов (мухоморы, 05.09–05.10) |
| Октябрь | Косметика | ✅ хеллоуин `hw26` готов (тыквы, 24.10–05.11) |
| Ноябрь | Престиж + подготовка НГ | престиж к моменту, когда ядро упрётся в потолок |
| Декабрь | `ny26` + мифики-тизер + макс. промо | eCPM ×2 — пик года, всё внимание сюда |

**Мем-конвейер (постоянно):** новый вирусный брейнрот в TikTok → за 48ч событийная цепочка (запись в `events.ts` + промпт в PROMPTS.md + пост владельца «мы добавили X»). Оригинальные персонажи «в духе», не копии — юридическая гигиена.

---

## 6. ЧЕГО НЕ ДЕЛАТЬ (проверено или продумано — не наступай)
- Не переписывай работающие системы ради красоты. Не трогай формат боя 5v5 (снежный ком — свойство, не баг).
- Не добавляй: энергию/жизни, копилку без бесплатного крана, лутбоксы за реальные деньги напрямую (сундуки за кристаллы — ок), таймеры-шантаж («награда сгорит через 10 минут»), скины существ за деньги.
- Не поднимай частоту interstitial выше капов. Не показывай офер `offline_vip`, пока игрок реально не упёрся в лимит времени (уже так — сохрани).
- Не мерь баланс цепочек winrate — только силой (бисекция в сим).
- Не делай фичей, требующих бэкенда, до порогов Фазы 4.
- Не верь headless-таймингам (2 FPS): паузы ≥1200мс, анимации не мерить.

## 7. ПРАВИЛА РАБОТЫ
1. Ветка `claude/yandex-games-plan-ke763a`, коммиты атомарные (сообщения по-английски), пуш после каждого логического блока, PR-описание держи актуальным.
2. Перед коммитом: `npx tsc --noEmit` + `npm run build`; экономика/бой — ещё `npm run sim` и сверка таблиц.
3. Баланс — только константами в `config.ts`/`arena.ts`. Строки — только через `t()`, сразу RU/EN/TR.
4. Никаких внешних CDN/запросов (модерация + CSP): всё бандлится или лежит в `public/`.
5. Общение и доки — русский; комментарии в коде — русский; коммиты — английский.
6. Каждый апдейт Фаз 1–3 — отдельный видимый релиз (алгоритм платформы любит частые обновления): фича + строка в README + короткий changelog для поста владельца в отчёте.
7. Новые счётчики и поля стейта — с миграцией в `ensureShapes()` и проверкой на старом сейве.

## 8. СЛЕДУЮЩЕЕ ДЕЙСТВИЕ
Фазы R (кроме смоука на телефоне и шагов владельца), 1, 2 и 3 закрыты целиком. Игра
содержательно готова к релизу и к месяцу еженедельных апдейтов.

Дальше по приоритету:
1. **Релизные хвосты:** смоук на реальном телефоне (FPS в бою, long-press, драг),
   интеграция арта владельца, когда PNG появятся в `public/skins/`.
2. **LiveOps-календарь** из §5: события до января в конфиге уже есть (`beach26`, `fall26`,
   `hw26`, `ny26`) — дальше нужны февраль-март и реакция на новые мемы. Правила, которые
   стоит держать: у нового события своя ФОРМА спрайта в `sprites.ts` (событие, отличающееся
   только цветом, не читается как апдейт), монеты в наградах — в `coinsMin` (минуты дохода),
   даты не должны пересекаться (`activeEvent` берёт первое подходящее и тихо спрячет второе),
   короткое событие (Хэллоуин — 12 дней) требует более жирных наград при тех же порогах,
   иначе его не успевают пройти.
3. **Фаза 4** (порты, настоящий PvP-бэкенд) — только по данным и порогам DAU.

Отдельно про честность, раз это уже дважды всплывало: и в кланах, и в боссах спек
предлагал показать игроку красивое число, которого нет (симулированный вклад друга,
«честные 50%» на лезвии ножа). Правильный ход в таких случаях — не «пометить бета», а
переделать фичу так, чтобы показывать только правду. Оба раза от этого выиграл и продукт.

Полезное из уже сделанного, на что можно опираться:
- `ui.panelsOpen()` — не показывай своё окно поверх чужого;
- `this.tip(key, target?, params?)` — баннер-подсказка с подстановками;
- `giveEgg(type)` — универсальная награда с очередью, годится для любых милстоунов;
- `S.stats` + `src/achievements.ts` — новые счётчики добавляй туда, награда появится сама;
- в headless-браузере игровые часы Phaser идут ~в 20 раз медленнее реального времени
  (сглаживание delta при ~2 FPS): `delayedCall(4000)` ждёт около 40 секунд стенных часов,
  а бой на 30 секунд — около 5 минут (с кнопкой ×2 — вдвое меньше);
- календарные фичи (турнир выходных, сезон, мутация) проверяются подменой `Date` через
  `addInitScript`: Phaser тикает по `performance.now`, поэтому игровой цикл не ломается;
- `frameByKey`/`frameRank` — рамки профиля не привязаны к лигам, добавляй свои;
- `SKINS` в config — скин поля добавляется одной записью (цвета сетки, рамки, акцента);
- `PASS.points` — очки журнала: подключай новые действия к треку одной строкой;
- в `npm run sim` есть блоки «ворота силы» для боссов и «возврат на плато» для сезона —
  меняешь боевые константы, сверяйся с ними, а не с интуицией.
