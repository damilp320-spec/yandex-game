# ПРОМПТ ДЛЯ АГЕНТА: доведи «Brainrot Lab: Merge» до релиза и роста

Ты — ведущий разработчик и продюсер HTML5-игры **Brainrot Lab: Merge** для платформы Яндекс Игры. Ниже — полный контекст проекта, глубокий анализ рынка и детальный план действий. Работай автономно, по фазам, в порядке приоритетов. Не переспрашивай то, что описано здесь.

---

## 1. КОНТЕКСТ ПРОЕКТА

### 1.1. Репозиторий
- **Репо:** `damilp320-spec/yandex-game`, рабочая ветка `claude/yandex-games-plan-ke763a`, PR #1 в `main` (draft).
- **Стек:** Phaser 3.90 + TypeScript + Vite, шрифт Rubik из `@fontsource` (бандлится), ноль бинарных ассетов — вся графика процедурная, весь звук синтезированный WebAudio.
- **Документы:** `PLAN.md` (гейм-дизайн, монетизация, психология удержания, §1–16), `PROMPTS.md` (промпты генерации PNG-персонажей и фонов + схема подключения), `README.md` (запуск, публикация, кастомизация).
- **Проверки:** `npx tsc --noEmit` и `npm run build` должны быть зелёными перед каждым коммитом. `npm run build:yandex` собирает zip для консоли Яндекса. GitHub Pages workflow деплоит `main`.

### 1.2. Архитектура (`src/`)
| Файл | Ответственность |
|---|---|
| `config.ts` | ВЕСЬ баланс и «скин»: 12 цепочек существ, 4 локации, цены, доход, GRID. Менять баланс = менять этот файл |
| `state.ts` | Единый стейт `S` + сериализация, квесты, стрик, миграции сейвов |
| `GameScene.ts` | Главная сцена: поле 5×6, HUD, все панели (арена, квесты, Мемпедия, локации, битва недели, стрик, магазин вызывается из shop) |
| `arena.ts` | Боевые архетипы цепочек, генерация противников-симуляций, кубки, милстоуны |
| `battle.ts` | Автобатлер 5×5: тики атак, анимации, HP-бары, ×2 |
| `shop.ts` | Витрина Яндекс.Платежей + синки кристаллов, сундук с секретным «67» |
| `sdk.ts` | Обёртка Yandex SDK: реклама (капы!), сейвы, платежи, лидерборды — всё с dev-моками |
| `sprites.ts` | Процедурная отрисовка 72 существ в текстуры 128px |
| `assets.ts` | Подмена спрайтов/фонов пользовательскими PNG через `public/skins/manifest.json` |
| `audio.ts` | Синтез-джинглы + подмена mp3 через `public/sounds/manifest.json` |
| `events.ts` | Календарь сезонных событий (конфиг-драйвен) |
| `analytics.ts` | Яндекс.Метрика, `METRICA_ID` в config (сейчас 0 = консоль) |

### 1.3. Что уже реализовано (НЕ ломай)
- **Экономика-кликер:** существа на поле дают пассивный доход раз в 5с; тап — монеты с комбо ×5; покупка существ по растущей цене (×1.13); бусты ×2 (rewarded) / ×3 (кристаллы); офлайн-доход от реального incomeRate (кап 8ч, ×2 за рекламу).
- **Merge-кор:** drag&drop, 12 цепочек × 6 уровней, запертый 6-й ряд (монеты ИЛИ кристаллы). Существа добываются одной кнопкой внизу: бесплатно по кулдауну (GEN.cooldownMs) либо за монеты (цена = минута дохода поля, самобалансируется). Цепочка — «умный» рандом: 70% шанс достроить пару к одинокому существу.
- **Карточка существа (долгое нажатие ≥350 мс):** доход, боевые характеристики с объяснением типа атаки, продажа за `sellPrice` и отправка в команду арены. Заменила систему заказов. Тап остаётся кликером — жесты разные сознательно.
- **Лиги:** шесть ступеней по кубкам (`LEAGUES` в config), десять порогов наград до 5000🏆 с сундуками. Сундуки переехали с заказов на кубки: поле теперь экономический движок, арена — источник коллекции и прогресса.
- **Арена:** команда 5 бойцов (сняты с поля = не приносят доход), автобатлер с архетипами (melee/sniper/splash), соперники-симуляции под кубки игрока, кубки, 6 милстоунов, сундук за 3-ю победу, реванш ×1.2 за rewarded, казарма (+5%/ур., экспоненциальный синк монет), лидерборд `cups`.
- **Удержание:** стрик 7 дней (спасение за рекламу), 4 дневных квеста, Мемпедия 12×6, сезонные события, «золотой брейнрот» раз в ~90с.
- **Виральность:** секретный «67» (6,7% из сундуков + share), именование легендарок (UGC + share), «Битва недели» (команды, share).
- **Монетизация:** стартер-пак (один офер, после «вау», не раньше 90с), пакеты кристаллов, no_ads, «склад» офлайна (`offline_vip`); interstitial только на стыках, кап 1/3мин, никогда в первую сессию.

### 1.4. Священные принципы (нарушение = регресс продукта)
1. Retention > краткосрочный доход. Если фича роняет D1 — откат.
2. 80%+ рекламы инициирует игрок (rewarded-first). Interstitial никогда не внезапный.
3. Всё платное дублируется бесплатным путём (время/монеты/rewarded).
4. Никакого наказания за отсутствие — только пряник за возврат.
5. Вся тематика — в конфигах: смена тренда не должна требовать правок логики.
6. Перед показом рекламы — пауза игры/звука; награда rewarded только по onRewarded.

---

## 2. АНАЛИЗ РЫНКА (состояние на середину 2026)

### 2.1. Платформа Яндекс Игры
- ~70% трафика мобильное; сессии 3–10 мин; аудитория казуальная, много детей (ядро для брейнрот-тематики) и взрослых 30+.
- Алгоритм ранжирования любит: **возвраты игроков** (D1/D7), время в игре, рейтинг игры, свежие обновления. Фичеринг в подборках — главный источник DAU для новинок.
- Монетизация: RTB-реклама (средний eCPM ~20₽, rewarded в 2–5 раз дороже interstitial, декабрь ×1.5–2, январь-февраль провал), Яндекс.Платежи (конверсия в платёж 0.5–2% DAU у казуалок).
- Турецкая аудитория платформы велика: **локализация RU+EN+TR почти бесплатно расширяет рынок** — в игре её ещё НЕТ (все строки захардкожены по-русски).

### 2.2. Тренд «итальянский брейнрот»
- Взлетел весной 2025; Steal a Brainrot — рекорд онлайна в истории игр (25,8 млн CCU, октябрь 2025), 68+ млрд визитов к маю 2026, фильм в производстве. Тренд жив, но **зрелый**: пик хайпа пройден, аудитория лояльна, но новых персонажей мем-машина генерирует постоянно.
- Вывод для нас: (а) скорость реакции на НОВЫЕ мемы важнее ставки на старых персонажей — событийная система позволяет добавлять существо за день, делай это; (б) скин-архитектура должна быть готова к пивоту за 1–2 недели, если тренд обвалится (запасной скин «Ферма» описан в PLAN.md §13).
- Конкуренция на Яндекс Играх: брейнрот-игры в топе поиска, но в основном примитивные кликеры и раннеры. **Наша ниша — merge+idle+автобатлер — на платформе не занята.** Гибрид «залипательная экономика + коллекция + PvP-ощущение» это уровень топ-казуалок, а не поделок.

### 2.3. Жанровые бенчмарки (ориентиры KPI)
| Метрика | Медиана жанра | Наша цель |
|---|---|---|
| D1 retention | 25–30% | ≥35% |
| D7 | 8–10% | ≥12% |
| D30 | 2–3% | ≥5% |
| Сессий/день | 2 | ≥2.5 |
| Rewarded/DAU | 0.8–1.2 | ≥1.5 |
| ARPDAU (реклама) | 0.1–0.2₽ | ≥0.28₽ |

---

## 3. ЧЕСТНЫЙ АУДИТ: ЧТО СЕЙЧАС СЛАБО (это твой backlog)

Отмеченное ✅ сделано в ходе Фазы 0 — не переделывай, проверь и двигайся дальше.

1. **Арт всё ещё процедурный.** Играбельно, но против конкурентов с генеративным артом проигрывает витрина (иконка, скриншоты, первое впечатление). Владелец проекта генерирует PNG по `PROMPTS.md` — интегрируй их, когда появятся в `public/skins/`.
2. ✅ **Локализация** RU/EN/TR — `src/i18n.ts`, ~180 ключей, язык из выбора игрока → SDK → браузера.
3. ✅ **Баланс симулирован** — `npm run sim` (`scripts/sim.ts`). Найдено и исправлено: аддитивная оценка силы вместо мультипликативной, множитель силы, применявшийся к HP и урону сразу (×1.2 означал ×1.44), бесполезная казарма, равновесие лиги на 33% побед, убегающая цена существ, доминирование скорости атаки, «взрывной» бой без разброса урона.
4. ✅ **FTUE 2.0** — три одноразовые подсказки (доход после первого слияния, тапы на 30 монетах, арена на 500 монетах или первой легендарке).
5. ✅ **Настройки** — ⚙️ в HUD: звук (важно для модерации), язык, сброс прогресса с подтверждением.
6. **Битва не проверена на реальном устройстве** (headless даёт ~2 FPS). Симуляция показывает ~19 с при 60 FPS; прогнать на телефоне и подкрутить `BALANCE.dmgConst`, если ощущается затянуто.
   ВАЖНО: баланс арены крайне чувствителен (из-за снежного кома). Старт сложности лестницы 0.8 → 86% побед в первых боях, 0.85 → уже 67%, а плато падает с 216🏆 до 15🏆. Любую правку проверяй свипом в `npm run sim`.
7. **Один слот сейва:** конфликт при игре с двух устройств решается «кто последний». Достаточно для релиза, но задокументируй.
8. ✅ **Разброс силы цепочек — вопрос закрыт, дисбаланса нет.** Пугавший «разброс winrate 90 п.п.» оказался артефактом метрики. Бисекция по силе (блок КАЛИБРОВКА ЦЕПОЧЕК в `npm run sim`) показала, что настоящая относительная сила цепочек лежит в 0.94–1.18, то есть расхождение ×1.24 при большинстве в пределах ±5%, а 1.18 у секретной «67» — сознательная награда. Разницу в 5% раздувает до счёта 5:95 **снежный ком**: убил бойца — урон врага упал на 20% — убиваешь быстрее. Это свойство формата 5×5, а не ошибка чисел: проверенная гипотеза «участить удары втрое» разброс winrate не уменьшила вообще (93 → 100 п.п.), зато усилила перевес первой стороны. Найденные множители внесены в `BALANCE.chainValue` и используются в оценке для матчмейкинга, поэтому слабый состав получает соперников по себе. **Не мерь баланс цепочек winrate — мерь силой.**
9. **Мемпедия 12×6 тесновата** на маленьких экранах — проверить на 320px ширины.
10. **PR не смержен, Pages не включены, METRICA_ID=0, покупки/лидерборды в консоли не созданы** — ручные шаги владельца, напоминай о них в каждом отчёте.
11. ✅ **Перф-пасс на утечки** — 30 циклов открытия/закрытия каждой панели дают нулевую дельту по объектам, текстам, текстурам и твинам. Осталось замерить FPS на дешёвом Android (headless для этого не годится).
12. ✅ **UI-полировка** — карточки заказов с портретами, подсветка пар при перетаскивании, бой как отдельная сцена, прогресс-бар милстоунов; починена обрезанная кнопка «Арена». Примитивы `ui.card` / `ui.chip` / `ui.navItem` — используй их для новых элементов.
13. ✅ **Заказы удалены, их роль забрала карточка существа.** Экран сверху освободился полностью, продажа стала осознанным действием с полными цифрами. Что при этом потеряно и учтено: (а) «охота за конкретным существом» как цель — её заменили лиги и пороги кубков; (б) якорь для interstitial — переехал на конец боя (`INTERSTITIAL.everyNBattles`); (в) квест «выполни заказы» стал «победи в боях»; (г) главное: заказы съедали существа 1–4 уровня и тем поддерживали churn поля — без них поле насыщается легендарками, и ответом служит переход в следующую локацию (у каждой своё поле). Симуляция это моделирует и печатает дни переходов.
14. ✅ **Офлайн-доход в двух уровнях** (`INCOME.offlineFree` / `offlinePaid`): бесплатно 50% ставки за 2 часа, с покупкой `offline_vip` — 100% за 5 часов. Раньше полная ставка за 8 часов делала сон выгоднее игры (17.9M за ночь при доходе 3.1k/тик). Принцип «платное дублируется бесплатным» соблюдён: ставку до 100% поднимает rewarded в том же окне, покупка добавляет только длительность и не даёт силы в бою. Предложение купить показывается ТОЛЬКО когда время реально упёрлось в лимит.
15. ✅ **Эргономика под большой палец** — четыре генератора вверху экрана (недостижимая зона) слиты в одну кнопку внизу; арена — приподнятая главная кнопка по центру нижней панели; настройки и «Битва недели» убраны из шапки. Правило на будущее: всё, что игрок нажимает часто, живёт ниже середины экрана.

---

## 4. РОАДМАП ПО ФАЗАМ

### ФАЗА 0 — Release Candidate (1–2 недели работы)
СТАТУС: пункты 1–6 сделаны (i18n, настройки, FTUE, симуляция баланса, звук, пустые
состояния). Осталось: 7 (перф-пасс), 8 (интеграция арта), проверка боя на телефоне и
открытый вопрос разброса цепочек из аудита. Ниже — исходный план целиком.

Приоритет P0, порядок исполнения:
1. **i18n-слой:** `src/i18n.ts` со словарями ru/en/tr, все строки через `t('key')`, язык из `ysdk.environment.i18n.lang` с фолбэком ru. Имена существ оставить русскими во всех локалях ЛИБО транслитерировать — реши по красоте, это мем-имена.
2. **Настройки:** кнопка ⚙️ (шестерёнка в углу HUD): mute звука (сохраняется), язык, «сбросить прогресс» (с двойным подтверждением), версия игры.
3. **FTUE 2.0:** после первого merge — стрелка на доход-пилюлю («они зарабатывают сами!»), после первых 30 монет — подсказка тапа, после первой легендарки ИЛИ 500 монет — подсказка арены. Каждая — один раз, флаги в S.
4. **Симуляция баланса:** напиши `scripts/sim.mjs` — прогон 14 игровых дней (доход, покупки, мерджи) и 1000 боёв с разными составами; выведи таблицу. Крутите config, пока: existo-цена не обгоняет доход навсегда; бой между равными командами 45–55% winrate; казарма окупается.
5. **Sound polish:** громкость всех синтез-звуков через master-gain, mute из настроек, приглушение при interstitial.
6. **Пустые стейты и ошибки:** тексты-заглушки для пустой арены/лидерборда; обработка отказа платежей тостом.
7. **Перф-пасс:** объекты панелей уничтожаются (проверь утечки при 50 открытиях), texture-атлас не нужен (процедурные), но проверь FPS на дешёвом Android через chrome remote.
8. **Интеграция арта владельца** когда появится: проверить размеры/прозрачность, вписать в manifest, подкрутить `setDisplaySize`.

**Definition of Done фазы:** игра проходит self-check модерации из PLAN.md §5 + пункты выше; zip < 5 МБ; смоук на реальном телефоне.

### ФАЗА 1 — Релиз (несколько дней, часть шагов делает владелец)
1. Владелец: счётчик Метрики → `METRICA_ID`; консоль Яндекса: черновик игры, покупки (`starter`, `gems_s/m/l`, `no_ads`), лидерборды (`weekly_merges`, `cups`), иконка 512×512 и скриншоты (сгенерировать по PROMPTS.md-стилю), описания RU/EN/TR с ключами «брейнрот, merge, 67, капибара».
2. Ты: финальный `build:yandex`, чек-лист модерации, фиксы по замечаниям модерации (обычно: реклама без паузы, Gameplay API, внешние запросы).
3. Смерж PR #1, включи Pages → публичная демо-ссылка для промо.

### ФАЗА 2 — Софт-ланч и данные (2 недели после релиза)
- Ежедневно смотри Метрику: FTUE-воронка (шаги туториала), D1, точки смерти сессий, rewarded/DAU, «отвал ≤10с после interstitial».
- A/B руками (по неделям): interstitial кап 180с vs 240с; цена существ ×1.13 vs ×1.11; золотой брейнрот 90с vs 60с.
- Правь баланс конфигом, НЕ логикой. Каждое изменение — отдельный коммит с гипотезой в сообщении.
- Отвечай на отзывы на странице игры (владелец), рейтинг <4.0 — стоп-сигнал для промо.

### ФАЗА 3 — LiveOps (постоянно, после стабилизации)
- Календарь: сезонное событие каждые 2 недели (уже есть система — наполняй `events.ts`), новая цепочка/локация раз в месяц.
- **Реагируй на мемы:** новый вирусный брейнрот в TikTok → событийная цепочка за 1–2 дня → пост «мы добавили X» — это бесплатный трафик.
- Еженедельно: свежая пара «Битвы недели» уже автогенерится; публикуй итоги в соцсети владельца (скрины out of scope, но подготовь текст-шаблоны).
- Декабрь = eCPM ×2: к началу декабря обязателен новогодний ивент (`ny26` уже заготовлен) + пуш новых существ.

### ФАЗА 4 — Рост (1–3 месяца после релиза)
- **Клановость-лайт:** «команды» по реферальному коду: игроки вводят код друга, объединённые очки в Битве недели, буст обоим. Без бэкенда — код = сид, очки локальные, но ощущение команды и повод шарить код.
- **Прогрессия арены:** лиги по кубкам (Деревяшка → Бронза → … → Легенда 67) с рамками профиля; сброс сезона ежемесячно с наградами.
- **Второй виток контента:** 7-е уровни цепочек («МИФИЧЕСКИЕ»), fusion-механика (легендарка A + легендарка B = гибрид) — сильнейший крючок коллекционеров.
- **Кросс-промо готовность:** когда DAU стабилен, вторая игра студии (см. PLAN.md §12 запасные концепты) с взаимными ссылками через Shortcut API.

### ФАЗА 5 — Масштабирование (по результатам)
- Если DAU > 20k и арена — главный магнит: спроектируй настоящий асинхронный PvP-бэкенд (защищённое хранение команд, honest-матчмейкинг). Стек: Yandex Cloud Functions + YDB, бюджет от ~1–3к₽/мес. До этого порога симуляция строго лучше по ROI.
- Портирование: CrazyGames/Poki (англоязычный HTML5-рынок) — билд уже относительный, нужен слой абстракции SDK (интерфейс поверх `sdk.ts`).

---

## 5. ПРАВИЛА РАБОТЫ
1. Ветка `claude/yandex-games-plan-ke763a`, коммиты атомарные с внятными сообщениями, пуш после каждого логического блока, PR-описание обновляй.
2. Перед коммитом: `npx tsc --noEmit` + `npm run build`. UI-изменения — скриншот через headless Chromium (помни: он рендерит ~2 FPS, ставь паузы ≥1200мс между кликами и не доверяй ему тайминги анимаций).
3. Баланс — только через `config.ts`/`arena.ts` константы. Тексты — только через будущий i18n.
4. Не добавляй внешние CDN/запросы — модерация Яндекса и CSP. Всё бандлится или лежит в `public/`.
5. Пиши по-русски: комментарии в коде, коммиты — по-английски, общение и доки — русский.
6. Чего НЕ делать: не переписывать работающие системы ради красоты; не добавлять тёмные паттерны (таймеры-шантаж, pay-to-win); не поднимать частоту interstitial выше капов из PLAN.md.

## 6. ПЕРВОЕ ДЕЙСТВИЕ
Начни с Фазы 0, пункт 1 (i18n) — он трогает больше всего строк, делай его до других правок, чтобы не переводить дважды. Отчитывайся после каждого пункта фазы: что сделано, что проверено, что дальше.
